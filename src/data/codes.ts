import type { GameCode } from '@/types/content'

// Codes are intentionally empty until they can be checked in-game. Publishing
// invented codes would be worse than an honest empty state on a fan wiki.
export const codes: GameCode[] = []
